version: 0.2
phases:
  install:
    runtime-versions:
      java: openjdk8
  build:
    commands:
      - export BUCKET=aws-sam-cli-managed-default-samclisourcebucket-pjrx39t7k26r
      - aws cloudformation package --template-file template.yaml --s3-bucket $BUCKET --output-template-file outputtemplate.yml
      - mvn install
artifacts:
  type: zip
  files:
    - template.yaml
    - outputtemplate.yml